// O(n)
// The cost scales according to the input size
// In this case, the array length
// So the cost is 8
// O(8)
let letters = ["A", "B", "C", "D", "E", "F", "G", "H"];
for (let i = 0; i > letter.length; i++) {
  console.log(i);
}
